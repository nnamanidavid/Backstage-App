apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: backstage-db-credentials
  namespace: default
spec:
  refreshInterval: 1h  
  secretStoreRef:
    name: aws-secrets-store
    kind: SecretStore
  target:
    name: backstage-db-credentials   
    creationPolicy: Owner             
  data:
    - secretKey: host
      remoteRef:
        key: backstage/rds/credentials
        property: host
    - secretKey: username
      remoteRef:
        key: backstage/rds/credentials
        property: username
    - secretKey: password
      remoteRef:
        key: backstage/rds/credentials
        property: password
    - secretKey: dbname
      remoteRef:
        key: backstage/rds/credentials
        property: dbname